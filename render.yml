services:
  - type: web
    name: bus-booking-system          # any slug-safe name
    env: docker                       # tells Render to use Dockerfile
    plan: free                        # or starter, standard, etc.
    dockerfilePath: ./Dockerfile      # path relative to repo root
    autoDeploy: true                  # re-deploy on every git push
